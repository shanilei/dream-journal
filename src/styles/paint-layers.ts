export const name = "paint-layers";

export const style = `
Create a heavily layered abstract painting. No figures, no faces, no silhouettes, no objects, no text.

STRUCTURE — OVERLAPPING COLOR PATCHES:
Build the image from 15–20 overlapping rectangular and organic color patches at varying angles and scales. Each patch is semi-transparent, allowing layers beneath to show through and mix. The combined result is a deep collage of color zones bleeding into each other — not a scene, not a place, just color and weight.

BRUSHWORK:
Each patch has rough, visible brushwork — gestural, imperfect, hand-made. Edges are soft and uneven, not clean or sharp. The paint surface is coarse and physical, with visible stroke marks layered on top of each other.

LAYERING DEPTH:
All layers remain visible simultaneously. Where two colors overlap they create a new mixed tone. Build 4–5 distinct depth levels — some patches feel buried deep beneath others, some float on top.

COMPOSITION:
No single element dominates. Patches collide across the full canvas — some tall and vertical, some wide and horizontal, some tilted. No clear focal point. The eye moves across the whole surface.

NOT: figures, faces, silhouettes, objects, symbols, text, clean digital edges, flat color fills, gradients without texture.
`.trim();
